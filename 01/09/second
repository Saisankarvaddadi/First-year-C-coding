#include<stdio.h>
 int factorial( int x);
void main()
{
    long int n,fact;
    printf("enter the value of n");
    scanf("%d",&n);
    fact=factorial(n);
    printf("%d",fact);
}
 int factorial( int x)
{
    if(x==1)
    return 1;
    else
    return x*factorial(x-1);
}
